streamlit==1.36.0
pandas==2.2.3
numpy==1.26.4
matplotlib==3.9.2
yfinance==0.2.57
plotly==5.24.1
bcrypt==4.2.1
statsmodels==0.14.4
scikit-learn==1.5.2
joblib==1.4.2
requests==2.32.3
curl-cffi==0.6.2
scipy==1.13.1
patsy==0.5.6
altair==4.2.2
protobuf==4.25.3
pyarrow==15.0.2
beautifulsoup4==4.12.3
lxml==5.2.2
pytest==8.3.4

# Lightweight optional-feature deps — always installed, but every feature
# built on them degrades gracefully (empty/Null result, no crash) if
# unavailable, e.g. no FRED key configured or the ticker has no news.
fredapi==0.5.2
vaderSentiment==3.3.2

# Heavier optional deps (LSTM model, MLflow experiment tracking) live in
# requirements-ml-extra.txt — see README. Not installed here by default so
# free-tier cloud deployments (Render/Streamlit Cloud) stay lightweight;
# the app runs fully without them, it just won't offer those two features.
